<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_area_listing"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_default"
    tools:context="com.fourtyonestudio.rcr.ui.activity.AreaListingActivity">


    <TextView
        android:id="@+id/tvCurrentDateArea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="@dimen/_4dp"
        android:background="@color/colorPrimary"
        android:gravity="center"
        android:padding="@dimen/_16dp"
        android:text="USER"
        android:textColor="@color/colorWhite"
        android:textSize="@dimen/f_24sp" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rvArea"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/tvCurrentDateArea"
        android:layout_marginLeft="@dimen/_4dp"
        android:layout_marginRight="@dimen/_4dp"
        android:background="@color/bg_default"
        android:clipToPadding="false"
        android:scrollbarSize="2dp"
        android:scrollbarThumbVertical="@color/colorPrimary"
        android:scrollbars="vertical" />


    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_margin="16dp"
        android:src="@drawable/add"
        app:borderWidth="0dp"
        app:fabSize="normal" />


</RelativeLayout>

